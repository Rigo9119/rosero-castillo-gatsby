import React from 'react';
import { StyledFooter } from './footer.styled';


const Footer = () => {
    return (
        <StyledFooter>
            footer
        </StyledFooter>
    );
};

Footer.propTypes = {

};

export default Footer

